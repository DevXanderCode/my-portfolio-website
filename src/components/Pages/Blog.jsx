import * as React from "react";

const Blog = (props) => {
    return(
        <div>
            <h4>
                Hi from the Blog component
            </h4>
        </div>
    )
}

export default Blog;